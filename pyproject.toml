[tool.pytest.ini_options]
testpaths = ["tests"]
markers = ["integration: requires local Docling models or Ollama"]

[tool.ruff]
line-length = 110
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]
